import 'package:flutter/material.dart';

class AddVehicle extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('Addd vehicle here '),
      ],
    );
  }
}